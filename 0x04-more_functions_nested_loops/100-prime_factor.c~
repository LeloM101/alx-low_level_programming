#include <stdio.h>
/**
* main - prints largest prime number of a number
* Return: Always 0
*/
int main(void)
{
int num = 612852475143, f, d;
for (f = 2; f < 612852475143; f++)
{
if (num % f = 0)
{
for (d < f)
if (f % d != 0)
scanf(f);
printf(f)
}
}
}
